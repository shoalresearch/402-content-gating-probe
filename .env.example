# Optional. Only needed for the authenticated TollBit rate probe.
# Never commit the real value.
TOLLBIT_API_KEY=

# Instead of exporting the key, the probe also accepts a path to a private env
# file that contains TOLLBIT_API_KEY. The file itself is never read into output.
TOLLBIT_ENV_FILE=
